# Official Blender MCP R8 Import-Order Follow-up Plan

> **For agentic workers:** execute this Plan task-by-task with fresh implementer and reviewer sessions.

**Goal:** Move the already-proven R7 probe constant below its complete import block, satisfy the quality gate without replaying the successful CLI probe, and perform the still-unconsumed live run.

**Architecture:** Freeze the R7 Task 2 BLOCKED terminal and its unique corrected CLI GREEN as external history. Derive one R8 probe from the exact R7 probe by moving one unchanged assignment block, and one R8 driver from the exact R7 driver by changing only this Plan path and one Git-parent block. Prove the relocation with exact raw-byte reversal plus pinned Ruff GREEN and old-layout E402 RED. Reuse the exact R6 controller, still-absent R6 evidence/followup-4 namespaces, and all inherited live/Option-C/review semantics.

## Frozen state and red lines

- Work only in `/Users/yeminjie/Developer/BlenderDesign/.worktrees/official-blender-mcp-install` on `codex/official-blender-mcp-install`.
- Clean base HEAD is R7 Plan commit `8be47eab75689b584dfb978e5c8e304fcca8927c`; parents are R6 `4765fa7104f6e538af36e67a09e726fb386b3e80`, R5 `507833f86949d90e8fbfbe90f8fcf749322ce19e`, R4 `81752dd10be750964d72cda9036db32e0cd2baf2`, and runbook `8d05ec975a6ba7317d5e9c23963233b2f2d11832`.
- R7 Plan is 570 lines, 33,209 bytes, SHA-256 `2c056df7e6b00365454f121fb1e4dc7bf6116c698a41c06483abe37ef008bd03`.
- R7 Task 2 report is dev/inode `16777232/302306296`, mode `0600`, nlink `1`, 1,446 bytes, SHA-256 `224c41afb6b9e7bde68927701e8f5f3d3d1c6e8dbfb975ec5c3adb1a13e30c2b`. It has one corrected CLI GREEN/count one, no live ticket, `actual_run_count: 0`, and unique `STATUS: BLOCKED`; the only failure is quality approval rejection. Never reopen, append, replace, unlink, or call it PASS.
- R7 spec review is inode `302306374`, 2,538 bytes, SHA `f9480429f33d67d465f3ca6ff2bf1deb28cb3c84359adbc457270b55cb5e176c`, unique `SPEC_REVIEW: APPROVED`. R7 quality review is inode `302306375`, 1,946 bytes, SHA `fd54a3e46f96c923f21940191e55d0cb6fbbde03eacdd17cae3c58fa0e0d2bea`, unique `QUALITY_REVIEW: REJECTED`, with sole Minor E402. Both are native-0600/nlink-1 and immutable.
- R7 runtime is dev/inode `16777232/302306297`, mode `0700`, with exactly three native-0600/nlink-1 leaves: `build.py` inode `302306298`, 11,795 bytes, SHA `58aa75df1fa968f26fc78114544b9ba0c14ade13cc3f566f740dcf98dc244cec`; `driver.sh` inode `302306301`, 32,904 bytes, SHA `0908a0dfe458f8f9ab0e7f20ae4b07b94ec4227d6f27ecf826412da1391e36a3`; `probe.py` inode `302306302`, 12,013 bytes, SHA `97c9619804fc741b56d1707ff92ed18fa3c4e685212edd60a6e3f7a4878a4190`. Never modify or launch R7 runtime.
- Exact R6 controller remains inode `302287509`, 285,770 bytes, SHA `c1a35f8f3e9c28429d4deb7f40315edd0f3c54a4cc620a8ee6ca96e5cc251cbc`. R6/R5 terminal history, old 16-leaf closure, config, Blender, source, and followup-3 remain exactly as R7 freezes them.
- `.superpowers/sdd/modeling-remediation/r6-evidence`, followup-4 brief/report, old-family attempt-0002, and every R8 file are absent. Port 9876 and owned Blender/controller/recorder processes are absent; host-managed stdio servers are out of scope.
- R7 already consumed the only corrected CLI probe and proved the configured absolute Blender path. R8 runs zero CLI probes and zero loader-only probes. Before Plan commit, create no R8 runtime. Before the live task, start no GUI/listener and create no evidence/ticket.
- No R8 controller, new evidence namespace, followup-5, attempt-0002, retry, env wrapper, config/source/test change, or R7 artifact mutation is permitted.

## File map

- Create/commit: `docs/superpowers/plans/2026-08-14-official-blender-mcp-r8-import-order.md`.
- Create ignored/bound: `.superpowers/sdd/modeling-remediation/r8-live-runtime/{build.py,probe.py,driver.sh}`.
- Create ignored/bound: `.superpowers/sdd/modeling-remediation/r8-task2-{report,spec-review,quality-review}.md`.
- Reuse only as absent names after Task 2: `.superpowers/sdd/modeling-remediation/r6-evidence/final-retest-r3/attempt-0001` and `task-7-followup-4-{brief,report}.md`.
- Create conditional followup-4 review artifacts exactly as R7/R6 defines.
- Modify only after certified success: `docs/audits/2026-08-10-official-blender-mcp-modeling-validation.md`.

### Task 1: certify and commit this Plan

- [ ] Prove the exact R7 Plan/runtime/report/reviews, R6 controller and older closure, absent namespaces/ticket, clean HEAD, empty port, and empty owned-process inventory.
- [ ] Extract Appendices A and B exactly and compile them in memory. Run Appendix B in a fresh native mode-0700 `/private/tmp` root. Require its unique GREEN: exact one-block probe relocation and raw reversal, pinned Ruff GREEN, old-layout E402 RED, exact two-anchor driver, Bash/heredoc/run shape, three source-mutation REDs, and parent-swap RED. Appendix B may not execute either probe or start Blender/MCP.
- [ ] Dispatch fresh spec/safety, execution/state-machine, and Ponytail/YAGNI lenses against one frozen Plan SHA. Any Critical, Important, or Minor burns the round; only this Plan may be repaired.
- [ ] Commit only this Plan with message `docs: plan probe import ordering`. Require parent `8be47eab75689b584dfb978e5c8e304fcca8927c`, one changed path, and clean status.

### Task 2: generate and statically certify the R8 probe/driver

- [ ] Before allocation, recheck every frozen R7/R6 artifact and absent live namespace. Root exclusive-creates the R8 Task 2 report/review files native-0600/nlink-1 and retains their original descriptors. Exclusive-create `r8-live-runtime` mode 0700. Extract Appendix A to `build.py`; generate exact `probe.py` and `driver.sh`. The directory contains exactly those three native-0600/nlink-1 leaves and no controller.
- [ ] On durable bytes require compile, exact raw reversal against the R7 probe, exact pinned Ruff GREEN, and the frozen Task 1 old-layout E402 RED result without rerunning it; require the driver exact two-anchor delta, `/bin/bash -n`, seven Python heredocs, exactly one controller `run`, and no run stdin redirect. Do not run R7/R8 probe, loader-only, CLI helper, Blender, MCP, GUI, or driver.
- [ ] Write a nonterminal Task 2 prefix binding all frozen R7 terminal/probe/review facts and R8 runtime evidence, with `inherited_r7_corrected_probe_run_count: 1`, `r8_cli_probe_run_count: 0`, `r8_loader_only_probe_run_count: 0`, and `actual_run_count: 0`. Fresh spec and quality reviewers write only through their original report descriptors. Root bounded-parses exact unique `SPEC_REVIEW: APPROVED` and `QUALITY_REVIEW: APPROVED`, fsync/fstat/hash/path-rechecks and closes each, records both identities in the Task 2 report, then alone appends `STATUS: PASS`, fsync/fstat/path-rechecks/closes, and freezes its identity. On any failure or rejection, root keeps the original Task 2 report descriptor open; fsync/fstat/hash/path-rechecks and closes every allocated review descriptor while recording its observed identity and verdict or exact empty/unavailable fact, appends those facts, the exact failure, all probe/run counts, and unique `STATUS: BLOCKED` through the original report descriptor, then fsync/fstat/path-rechecks/closes and freezes the report. Never close before the terminal append or reopen a report. Recheck all frozen artifacts, absent namespaces/ticket, clean Git state, empty port, and empty owned-process inventory after generation, after each review, and immediately before either terminal append. No Task 2 retry exists.

### Task 3: perform the sole fresh live run

- [ ] Before allocating followup-4, descriptor-bind the R8 Task 2 PASS report and both approvals, requiring equality to the recorded identities and exact relocation/Ruff facts plus inherited CLI count one, R8 CLI count zero, R8 loader-only count zero, and live-run count zero. Bind the immutable R7 BLOCKED report/reviews/runtime and unique inherited CLI GREEN as external history, never as a second PASS or fresh evidence.
- [ ] Supersede only R7 Task 3's admission requirement that R7 Task 2 be PASS and not BLOCKED: require the R8 PASS/approvals above and the real immutable R7 BLOCKED history instead. Apply every remaining R7 Task 3 transition exactly, substituting this Plan/commit/SHA and exact R8 driver path/SHA. Reuse the exact R6 controller and still-absent R6 evidence/followup-4 names. Never execute either probe or the R7 driver. Preserve one live PTY/ticket, prelaunch/FD/cleanup checks, and old-closure validation.
- [ ] Option C remains unchanged: at `VISUAL_ACK_REQUIRED`, implementer returns only live PTY identity and every ordered absolute PNG path/SHA; root displays every image and user supplies every verdict before one canonical ACK.

### Task 4: independently review a grouped direct failure

- [ ] Apply R7 Task 4 with this Plan/driver and R8 Task 2 bindings. R7/R8 Task 2 and all historical artifacts remain external and forbidden from the fresh review package.

### Task 5: audit and commit a successful Option-C run

- [ ] Apply R7 Task 5 with this Plan/driver and R8 Task 2 bindings. Replace only its first-parent rule with exact length seven in runbook/R4/R5/R6/R7/R8/audit order. Seven length-one `paths` arrays map to those files; parent commits are old-R3/runbook/R4/R5/R6/R7/R8 respectively.

## Appendix A: exact R8 runtime builder

Extract the following Python body without fences. Exact identity: 181 lines / 7,483 bytes / SHA-256 `2d86be69ef940ca53b83cf0fe590cd57bffa902bdb0e9b2b2ca2c87049f00299`. It accepts `SOURCE_R7_DRIVER SOURCE_R7_PROBE OUTPUT_DRIVER OUTPUT_PROBE`.

````python
from __future__ import annotations

import hashlib
import os
import stat
import sys
from pathlib import Path


SOURCE_DRIVER_ID = (16777232, 302306301)
SOURCE_DRIVER_SIZE = 32904
SOURCE_DRIVER_SHA256 = "0908a0dfe458f8f9ab0e7f20ae4b07b94ec4227d6f27ecf826412da1391e36a3"
SOURCE_PROBE_ID = (16777232, 302306302)
SOURCE_PROBE_SIZE = 12013
SOURCE_PROBE_SHA256 = "97c9619804fc741b56d1707ff92ed18fa3c4e685212edd60a6e3f7a4878a4190"
OUTPUT_DRIVER_SIZE = 33034
OUTPUT_DRIVER_SHA256 = "d1e808d55cc320ae607809639f33e973184d50023d2809fe237fc64810595211"
OUTPUT_PROBE_SIZE = 12013
OUTPUT_PROBE_SHA256 = "1a2e050ab80aa1bf76e92c8ca8a9a0784136379807db4f3532d04b0101bedafe"
OLD_PLAN = "docs/superpowers/plans/2026-08-14-official-blender-mcp-r7-probe-loader.md"
NEW_PLAN = "docs/superpowers/plans/2026-08-14-official-blender-mcp-r8-import-order.md"
OLD_TOPOLOGY = (
    '  test "$(git -C "$FEATURE_ROOT" rev-parse "$R3_PLAN_COMMIT^")" = '
    "4765fa7104f6e538af36e67a09e726fb386b3e80\n"
    '  test "$(git -C "$FEATURE_ROOT" rev-parse '
    '4765fa7104f6e538af36e67a09e726fb386b3e80^)" = '
    "507833f86949d90e8fbfbe90f8fcf749322ce19e"
)
NEW_TOPOLOGY = (
    '  test "$(git -C "$FEATURE_ROOT" rev-parse "$R3_PLAN_COMMIT^")" = '
    "8be47eab75689b584dfb978e5c8e304fcca8927c\n"
    '  test "$(git -C "$FEATURE_ROOT" rev-parse '
    '8be47eab75689b584dfb978e5c8e304fcca8927c^)" = '
    "4765fa7104f6e538af36e67a09e726fb386b3e80\n"
    '  test "$(git -C "$FEATURE_ROOT" rev-parse '
    '4765fa7104f6e538af36e67a09e726fb386b3e80^)" = '
    "507833f86949d90e8fbfbe90f8fcf749322ce19e"
)
OLD_IMPORT_BLOCK = (
    "import sys\nimport types\n\n\n"
    'MODULE_NAME = "_r7_controller_probe_c1a35f8f"\n'
    "from pathlib import Path\n\n"
    "from blmcp.tools_helpers.blender_cli import _get_blender_path, run_blender_cli\n"
)
NEW_IMPORT_BLOCK = (
    "import sys\nimport types\nfrom pathlib import Path\n\n"
    "from blmcp.tools_helpers.blender_cli import _get_blender_path, run_blender_cli\n\n\n"
    'MODULE_NAME = "_r7_controller_probe_c1a35f8f"\n'
)


def file_id(info: os.stat_result) -> tuple[int, ...]:
    return info.st_dev, info.st_ino, info.st_uid, stat.S_IMODE(info.st_mode), info.st_nlink, info.st_size, info.st_mtime_ns, info.st_ctime_ns


def parent_id(info: os.stat_result) -> tuple[int, ...]:
    return info.st_dev, info.st_ino, info.st_uid, stat.S_IMODE(info.st_mode)


def read_owned(path: Path, size: int, digest: str, dev_ino: tuple[int, int]) -> bytes:
    info = os.lstat(path)
    identity = file_id(info)
    if (
        path != Path(os.path.realpath(path))
        or not stat.S_ISREG(info.st_mode)
        or stat.S_ISLNK(info.st_mode)
        or info.st_uid != os.getuid()
        or stat.S_IMODE(info.st_mode) != 0o600
        or info.st_nlink != 1
        or info.st_size != size
        or identity[:2] != dev_ino
    ):
        raise RuntimeError("unsafe R8 input")
    fd = os.open(path, os.O_RDONLY | os.O_NOFOLLOW)
    try:
        if file_id(os.fstat(fd)) != identity:
            raise RuntimeError("R8 input changed before open")
        raw = bytearray()
        while len(raw) < size:
            chunk = os.read(fd, min(65536, size - len(raw)))
            if not chunk:
                break
            raw.extend(chunk)
        payload = bytes(raw)
        if len(payload) != size or os.read(fd, 1) or hashlib.sha256(payload).hexdigest() != digest:
            raise RuntimeError("R8 input bytes differ")
        if file_id(os.fstat(fd)) != identity:
            raise RuntimeError("R8 input changed during read")
    finally:
        os.close(fd)
    if file_id(os.lstat(path)) != identity:
        raise RuntimeError("R8 input path changed")
    return payload


def once(text: str, old: str, new: str) -> str:
    if text.count(old) != 1 or old == new:
        raise RuntimeError("R8 anchor is not unique")
    return text.replace(old, new, 1)


def build_driver(raw: bytes) -> bytes:
    text = raw.decode()
    text = once(text, OLD_PLAN, NEW_PLAN)
    text = once(text, OLD_TOPOLOGY, NEW_TOPOLOGY)
    payload = text.encode()
    if len(payload) != OUTPUT_DRIVER_SIZE or hashlib.sha256(payload).hexdigest() != OUTPUT_DRIVER_SHA256:
        raise RuntimeError("R8 driver identity differs")
    return payload


def build_probe(raw: bytes) -> bytes:
    text = once(raw.decode(), OLD_IMPORT_BLOCK, NEW_IMPORT_BLOCK)
    payload = text.encode()
    if len(payload) != OUTPUT_PROBE_SIZE or hashlib.sha256(payload).hexdigest() != OUTPUT_PROBE_SHA256:
        raise RuntimeError("R8 probe identity differs")
    return payload


def write_owned(path: Path, payload: bytes) -> None:
    if path != Path(os.path.realpath(path)):
        raise RuntimeError("R8 output path is not canonical")
    parent = path.parent
    before = os.lstat(parent)
    expected = parent_id(before)
    if not stat.S_ISDIR(before.st_mode) or stat.S_ISLNK(before.st_mode) or before.st_uid != os.getuid() or stat.S_IMODE(before.st_mode) != 0o700:
        raise RuntimeError("R8 output parent is unsafe")
    parent_fd = os.open(parent, os.O_RDONLY | os.O_DIRECTORY | os.O_NOFOLLOW)
    created: tuple[int, int] | None = None
    success = False
    try:
        if parent_id(os.fstat(parent_fd)) != expected:
            raise RuntimeError("R8 output parent changed")
        fd = os.open(path.name, os.O_WRONLY | os.O_CREAT | os.O_EXCL | os.O_NOFOLLOW, 0o600, dir_fd=parent_fd)
        try:
            opened = os.fstat(fd)
            created = opened.st_dev, opened.st_ino
            view = memoryview(payload)
            while view:
                count = os.write(fd, view)
                if count <= 0:
                    raise RuntimeError("short R8 write")
                view = view[count:]
            os.fsync(fd)
            final = os.fstat(fd)
            if (final.st_dev, final.st_ino) != created or final.st_uid != os.getuid() or stat.S_IMODE(final.st_mode) != 0o600 or final.st_nlink != 1 or final.st_size != len(payload):
                raise RuntimeError("R8 output identity differs")
        finally:
            os.close(fd)
        leaf = os.stat(path.name, dir_fd=parent_fd, follow_symlinks=False)
        if (leaf.st_dev, leaf.st_ino) != created:
            raise RuntimeError("R8 output path changed")
        os.fsync(parent_fd)
        if parent_id(os.lstat(parent)) != expected:
            raise RuntimeError("R8 output parent changed after create")
        success = True
    finally:
        if not success and created is not None:
            try:
                leaf = os.stat(path.name, dir_fd=parent_fd, follow_symlinks=False)
                if (leaf.st_dev, leaf.st_ino) == created:
                    os.unlink(path.name, dir_fd=parent_fd)
            except FileNotFoundError:
                pass
        os.close(parent_fd)


def main() -> int:
    if len(sys.argv) != 5:
        raise SystemExit("usage: build.py SOURCE_R7_DRIVER SOURCE_R7_PROBE OUTPUT_DRIVER OUTPUT_PROBE")
    source_driver, source_probe, output_driver, output_probe = map(Path, sys.argv[1:])
    driver = build_driver(read_owned(source_driver, SOURCE_DRIVER_SIZE, SOURCE_DRIVER_SHA256, SOURCE_DRIVER_ID))
    probe = build_probe(read_owned(source_probe, SOURCE_PROBE_SIZE, SOURCE_PROBE_SHA256, SOURCE_PROBE_ID))
    write_owned(output_driver, driver)
    write_owned(output_probe, probe)
    print(f"R8_RUNTIME_GREEN driver_sha256={OUTPUT_DRIVER_SHA256} probe_sha256={OUTPUT_PROBE_SHA256}")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
````

## Appendix B: exact disposable R8 harness

Extract the following Python body without fences. Exact identity: 134 lines / 6,255 bytes / SHA-256 `af14a4a2ef5d0110f702a8f2af6f3106bdddcf217086681661570334732ee57b`. Run `harness.py BUILD_PY SOURCE_DRIVER SOURCE_PROBE OUTPUT_ROOT` with canonical absolute paths and an empty native mode-0700 output root.

````python
from __future__ import annotations

import hashlib
import os
import re
import stat
import subprocess
import sys
from pathlib import Path


BUILD_SHA256 = "2d86be69ef940ca53b83cf0fe590cd57bffa902bdb0e9b2b2ca2c87049f00299"
SOURCE_DRIVER_SHA256 = "0908a0dfe458f8f9ab0e7f20ae4b07b94ec4227d6f27ecf826412da1391e36a3"
SOURCE_PROBE_SHA256 = "97c9619804fc741b56d1707ff92ed18fa3c4e685212edd60a6e3f7a4878a4190"
OUTPUT_DRIVER_SHA256 = "d1e808d55cc320ae607809639f33e973184d50023d2809fe237fc64810595211"
OUTPUT_PROBE_SHA256 = "1a2e050ab80aa1bf76e92c8ca8a9a0784136379807db4f3532d04b0101bedafe"
UVX = "/Users/yeminjie/.local/bin/uvx"


def owned(path: Path, digest: str) -> bytes:
    info = os.lstat(path)
    if path != Path(os.path.realpath(path)) or not stat.S_ISREG(info.st_mode) or info.st_uid != os.getuid() or stat.S_IMODE(info.st_mode) != 0o600 or info.st_nlink != 1 or info.st_size > 1024 * 1024:
        raise RuntimeError("unsafe R8 harness input")
    raw = path.read_bytes()
    if hashlib.sha256(raw).hexdigest() != digest:
        raise RuntimeError("R8 harness digest differs")
    return raw


def probe_shape(source: bytes, output: bytes, namespace: dict[str, object]) -> None:
    old = namespace["OLD_IMPORT_BLOCK"].encode()
    new = namespace["NEW_IMPORT_BLOCK"].encode()
    if source.count(old) != 1 or output.count(new) != 1 or output.replace(new, old, 1) != source:
        raise RuntimeError("probe raw relocation differs")


def ruff(path: Path, green: bool) -> None:
    result = subprocess.run(
        [UVX, "--quiet", "ruff@0.16.2", "check", "--no-cache", "--isolated", "--target-version", "py313", "--select", "E4,E7,E9,F", str(path)],
        text=True,
        capture_output=True,
        timeout=30,
        check=False,
    )
    if green and result.returncode:
        raise RuntimeError(f"R8 Ruff failed: {result.stdout}{result.stderr}")
    if not green and (result.returncode == 0 or "E402" not in result.stdout + result.stderr):
        raise RuntimeError("old E402 layout was not rejected")


def require_red(call, label: str) -> None:
    try:
        call()
    except (OSError, RuntimeError, SyntaxError):
        return
    raise RuntimeError(f"R8 negative accepted: {label}")


def main() -> int:
    if len(sys.argv) != 5:
        raise SystemExit("usage: harness.py BUILD_PY SOURCE_DRIVER SOURCE_PROBE OUTPUT_ROOT")
    build, source_driver, source_probe, root = map(Path, sys.argv[1:])
    build_raw = owned(build, BUILD_SHA256)
    driver_raw = owned(source_driver, SOURCE_DRIVER_SHA256)
    probe_raw = owned(source_probe, SOURCE_PROBE_SHA256)
    compile(build_raw, str(build), "exec")
    namespace: dict[str, object] = {"__name__": "r8_build_harness"}
    exec(compile(build_raw, str(build), "exec"), namespace)
    expected_driver = namespace["build_driver"](driver_raw)
    expected_probe = namespace["build_probe"](probe_raw)
    info = os.lstat(root)
    if root != Path(os.path.realpath(root)) or not stat.S_ISDIR(info.st_mode) or info.st_uid != os.getuid() or stat.S_IMODE(info.st_mode) != 0o700 or any(root.iterdir()):
        raise RuntimeError("unsafe R8 harness root")
    output_driver = root / "driver.sh"
    output_probe = root / "probe.py"
    result = subprocess.run([sys.executable, str(build), str(source_driver), str(source_probe), str(output_driver), str(output_probe)], text=True, capture_output=True, timeout=20, check=False)
    if result.returncode or result.stdout.count("R8_RUNTIME_GREEN ") != 1:
        raise RuntimeError(f"R8 build failed: {result.stderr}")
    if owned(output_driver, OUTPUT_DRIVER_SHA256) != expected_driver or owned(output_probe, OUTPUT_PROBE_SHA256) != expected_probe:
        raise RuntimeError("R8 output differs")
    compile(expected_probe, "<r8-probe>", "exec")
    probe_shape(probe_raw, expected_probe, namespace)
    ruff(source_probe, False)
    ruff(output_probe, True)
    subprocess.run(["/bin/bash", "-n", str(output_driver)], timeout=10, check=True)
    heredocs = re.findall(rb"<<'PY'\n(.*?)\nPY(?:\n|$)", expected_driver, re.DOTALL)
    if len(heredocs) != 7:
        raise RuntimeError("R8 heredoc count differs")
    for number, body in enumerate(heredocs, 1):
        compile(body, f"<r8-heredoc-{number}>", "exec")
    if expected_driver.count(b'"$ATTEMPT_ROOT/r3_controller.py" run \\\n') != 1:
        raise RuntimeError("R8 controller run count differs")
    builder_text = build_raw.decode()
    mutations = (
        builder_text.replace("    text = once(text, OLD_PLAN, NEW_PLAN)\n", "    text = text\n", 1),
        builder_text.replace("    text = once(text, OLD_TOPOLOGY, NEW_TOPOLOGY)\n", "    text = text\n", 1),
        builder_text.replace("    text = once(raw.decode(), OLD_IMPORT_BLOCK, NEW_IMPORT_BLOCK)\n", "    text = raw.decode()\n", 1),
    )
    for number, text in enumerate(mutations, 1):
        if text == builder_text:
            raise RuntimeError("R8 builder mutation unchanged")
        ns: dict[str, object] = {"__name__": f"r8_mutation_{number}"}
        exec(compile(text, f"<r8-mutation-{number}>", "exec"), ns)
        if number < 3:
            require_red(lambda ns=ns: ns["build_driver"](driver_raw), f"driver-{number}")
        else:
            require_red(lambda ns=ns: ns["build_probe"](probe_raw), "probe-3")
    swap = root / "swap"
    swap.mkdir(mode=0o700)
    held = root / "held"
    original_open = os.open
    changed = False

    def swapping_open(path, flags, mode=0o777, *, dir_fd=None):
        nonlocal changed
        if not changed and path == "probe.py" and flags & os.O_CREAT and dir_fd is not None:
            changed = True
            os.rename(swap, held)
            os.mkdir(swap, 0o700)
        return original_open(path, flags, mode, dir_fd=dir_fd)

    os.open = swapping_open
    try:
        require_red(lambda: namespace["write_owned"](swap / "probe.py", expected_probe), "parent-swap")
    finally:
        os.open = original_open
    if not changed or (swap / "probe.py").exists() or (held / "probe.py").exists():
        raise RuntimeError("R8 parent-swap left output")
    print("R8_PLAN_HARNESS_GREEN probe_move=1 ruff_green=1 old_e402_red=1 driver=1 source_mutations_red=3 parent_swap_red=1")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
````
