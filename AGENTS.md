# Graft Map

- 任何产生仓库文件变更的任务，必须在最后一次修改后运行 `graft build .`；若此后再次修改仓库文件，必须重新运行。
- 最终交付前必须运行 `graft check .`，且退出码为 0；否则不得表述任务已完成。
- 默认只构建 wiring 图；除非用户明确要求深层语义地图，否则不使用 `--deep`。
- `graft/` 是可再生的本地缓存，不暂存、不提交。
